spring:
    main:
        banner-mode: off
    application:
        name: api
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password:

logging:
    level:
        root: FATAL

app:
    services:
        authorization:
            url: ${AUTHORIZATION_SERVICE_URL:http://127.0.0.1:12345/api/v0/authorize}
        notification:
            url: ${NOTIFICATION_SERVICE_URL:http://127.0.0.1:12345/api/v0/notify}