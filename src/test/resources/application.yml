spring:
    main:
        banner-mode: off
    application:
        name: api
    datasource:
        url: jdbc:h2:mem:testdb
        username: sa
        password:

logging:
    level:
        root: FATAL
